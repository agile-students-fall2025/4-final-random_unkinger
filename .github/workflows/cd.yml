name: Deploy to prod
on:
  push:
    branches:
      - master # only deploy on master change

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: secure copy files to droplet
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USER }}
          key: ${{ secrets.DO_SSH_PRIVATE_KEY }}
          source: "back-end,front-end,docker-compose.yml"
          target: "/var/www/nutri"

      - name: ssh and deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USER }}
          key: ${{ secrets.DO_SSH_PRIVATE_KEY }}
          command_timeout: 30m
          script: |
            cd /var/www/nutri
            echo "${{ secrets.ENV_FILE_PRODUCTION }}" > .env
            docker-compose down
            docker-compose --env-file ./.env up --build -d
            docker image prune -f

